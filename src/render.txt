# This is the main compatibility layer between Cython (rcast.pyx) and Python 3
# (pyglet).


class Render:

# VARIABLES #

# 2D array of pyglet pixels
screen

# METHODS #

# Look at screen and draw on pyglet accordingly.
def redraw(some_pyglet_parameters)

# This module will definitely need more stuff, but I've been learning more
# Cython than Pyglet recently. :P
